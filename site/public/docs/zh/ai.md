# AI 助手

接入你自己的模型接口，让 AI 在写作时搭把手。

## 配置

1. 在「设置 › AI」填入 OpenAI 兼容接口的地址与密钥。
2. 可自定义模型名与温度等参数；支持自建/第三方兼容端点。

## 用法

- **行内补全**：开启后，写作时给出灰色幽灵建议，按 Tab 采纳。
- **AI 面板**：选中文字唤出，进行续写、润色、翻译、总结等操作。
- **斜杠动作**：在空行输入 `/` 触发常用 AI 指令。

> 密钥仅保存在本地，请求直连你配置的服务，Mox 不经手你的数据。
